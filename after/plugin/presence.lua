require("presence").setup({
	auto_upate = true,
	neovim_image_text = "The only editor for not weak",
	enable_line_number = true,
	buttons = {
		{
			label = "Me",
			url = "https://slashrichie.dev/"
		}
	}
})
